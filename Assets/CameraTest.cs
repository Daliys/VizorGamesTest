
using UnityEngine;

public class CameraTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
      // /  print(Camera.main.rect);
       // print(Camera.main.WorldToScreenPoint(transform.position) + "  " + Camera.main.ViewportToWorldPoint(Camera.main.transform.position)+ "   \\  "  + Camera.main.scaledPixelHeight + "  " + Camera.main.scaledPixelWidth) ;
        //print();
    }
}
